<UserControl x:Class="KicsitLibrary.Desktop.Views.FacultyStaffClearanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:KicsitLibrary.Desktop"
             Background="Transparent">
    <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="3*"/><ColumnDefinition Width="2*"/></Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,10" Padding="12">
                <StackPanel>
                    <WrapPanel>
                        <StackPanel Width="210" Margin="0,0,8,8">
                            <TextBlock Text="Personnel Number / Name" FontSize="11" FontWeight="SemiBold"/>
                            <TextBox Style="{StaticResource StyledTextBox}" Text="{Binding SearchText}"/>
                        </StackPanel>
                        <StackPanel Width="140" Margin="0,0,8,8">
                            <TextBlock Text="Department" FontSize="11" FontWeight="SemiBold"/>
                            <TextBox Style="{StaticResource StyledTextBox}" Text="{Binding Department}"/>
                        </StackPanel>
                        <StackPanel Width="145" Margin="0,0,8,8">
                            <TextBlock Text="Faculty Type" FontSize="11" FontWeight="SemiBold"/>
                            <ComboBox ItemsSource="{Binding FacultyTypeOptions}" SelectedItem="{Binding SelectedFacultyType}"/>
                        </StackPanel>
                        <StackPanel Width="125" Margin="0,0,8,8">
                            <TextBlock Text="Clearance" FontSize="11" FontWeight="SemiBold"/>
                            <ComboBox ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding SelectedStatus}"/>
                        </StackPanel>
                    </WrapPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
                        <Button Content="Clear Filters" Command="{Binding ClearFiltersCommand}" Style="{StaticResource SecondaryButton}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            <DataGrid Grid.Row="1" ItemsSource="{Binding Members}" SelectedItem="{Binding SelectedMember}"
                      AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Personnel No" Binding="{Binding PersonnelNumber}" Width="115"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding FacultyType}" Width="115"/>
                    <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="100"/>
                    <DataGridTextColumn Header="Designation" Binding="{Binding Designation}" Width="110"/>
                    <DataGridTextColumn Header="Clearance" Binding="{Binding ClearanceStatus}" Width="95"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Faculty &amp; Staff Clearance Review" FontWeight="Bold" FontSize="16"
                               Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding SelectedMember.Name}" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Text="{Binding SelectedMember.PersonnelNumber}" Foreground="{StaticResource TextMutedBrush}"/>
                    <UniformGrid Columns="3" Margin="0,12,0,10">
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,5,0"><StackPanel><TextBlock Text="Books" FontSize="10"/><TextBlock Text="{Binding CheckResult.PendingBooksCount}" FontSize="20" FontWeight="Bold"/></StackPanel></Border>
                        <Border Style="{StaticResource CardStyle}" Margin="5,0"><StackPanel><TextBlock Text="Fine" FontSize="10"/><TextBlock Text="{Binding CheckResult.PendingFineAmount, StringFormat={}{0:N2}}" FontSize="20" FontWeight="Bold"/></StackPanel></Border>
                        <Border Style="{StaticResource CardStyle}" Margin="5,0,0,0"><StackPanel><TextBlock Text="Loss/Damage" FontSize="10"/><TextBlock Text="{Binding CheckResult.LostOrDamagedCaseCount}" FontSize="20" FontWeight="Bold"/></StackPanel></Border>
                    </UniformGrid>
                    <TextBlock Text="{Binding CheckResult.Message}" TextWrapping="Wrap" Margin="0,0,0,8"/>
                    <DataGrid ItemsSource="{Binding CheckResult.BlockingItems}" Height="150" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Block" Binding="{Binding BlockType}" Width="100"/>
                            <DataGridTextColumn Header="Accession" Binding="{Binding AccessionNumber}" Width="90"/>
                            <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <TextBlock Text="Approval Remarks" FontWeight="SemiBold" Margin="0,10,0,3"/>
                    <TextBox Style="{StaticResource StyledTextBox}" Text="{Binding Remarks}" AcceptsReturn="True" Height="55"/>
                    <TextBlock Text="Revoke Reason" FontWeight="SemiBold" Margin="0,8,0,3"/>
                    <TextBox Style="{StaticResource StyledTextBox}" Text="{Binding RevokeReason}"/>
                    <WrapPanel Margin="0,10,0,0">
                        <Button Content="Check Clearance" Command="{Binding CheckClearanceCommand}" ToolTip="Check active issues, pending fines, and unresolved reservations." Style="{StaticResource PrimaryButton}" Margin="0,0,6,6"/>
                        <Button Content="Approve" Command="{Binding ApproveClearanceCommand}" ToolTip="Approve clearance only when all current dues are resolved." Style="{StaticResource PrimaryButton}" Margin="0,0,6,6"/>
                        <Button Content="Revoke" Command="{Binding RevokeClearanceCommand}" ToolTip="Revoke a previous clearance and record the reason." Style="{StaticResource DangerButton}" Margin="0,0,6,6"/>
                        <Button Content="Generate Certificate" Command="{Binding GenerateCertificateCommand}" ToolTip="Create a branded PDF clearance certificate after revalidation." Style="{StaticResource SecondaryButton}" Margin="0,0,6,6"/>
                        <Button Content="Borrowing History" Command="{Binding OpenBorrowingHistoryCommand}" ToolTip="Open the selected person's full borrowing history." Style="{StaticResource SecondaryButton}"/>
                    </WrapPanel>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource SuccessBrush}" TextWrapping="Wrap" Margin="0,8,0,0"/>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap"
                               Visibility="{Binding ErrorMessage, Converter={local:StringVisibilityConverter}}"/>
                    <ProgressBar Height="4" IsIndeterminate="True" Margin="0,8,0,0"
                                 Visibility="{Binding IsBusy, Converter={local:BooleanToVisibilityConverter}}"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
