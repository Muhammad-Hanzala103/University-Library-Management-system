<UserControl x:Class="KicsitLibrary.Desktop.Views.ReportPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:KicsitLibrary.Desktop"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="{Binding ReportTitle}" FontSize="16" FontWeight="Bold"
                       Foreground="{StaticResource PrimaryBrush}"/>
            <TextBlock DockPanel.Dock="Right"
                       Text="{Binding TotalCount, StringFormat='{}{0} record(s)'}"
                       Foreground="{StaticResource TextMutedBrush}"/>
        </DockPanel>

        <Grid Grid.Row="1">
            <DataGrid ItemsSource="{Binding PreviewRows}"
                      AutoGenerateColumns="True"
                      IsReadOnly="True"
                      HorizontalScrollBarVisibility="Auto"
                      Visibility="{Binding IsEmpty, Converter={local:BooleanToVisibilityConverter}, ConverterParameter=Inverse}"/>
            <Border Style="{StaticResource CardStyle}"
                    Visibility="{Binding IsEmpty, Converter={local:BooleanToVisibilityConverter}}">
                <TextBlock Text="{Binding PreviewMessage}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextMutedBrush}"
                           FontSize="14"/>
            </Border>
        </Grid>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding SummaryItems}" Margin="0,10,0,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource BgBrush}" CornerRadius="4"
                            Padding="10,6" Margin="0,0,8,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Key}" FontWeight="SemiBold"/>
                            <TextBlock Text=": "/>
                            <TextBlock Text="{Binding Value}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
