<UserControl x:Class="KicsitLibrary.Desktop.Views.ReservationManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="UserControl.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="MainGrid"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.4"
                                     DecelerationRatio="0.8"/>
                    <ThicknessAnimation Storyboard.TargetName="MainGrid"
                                        Storyboard.TargetProperty="Margin"
                                        From="0,15,0,0" To="0,0,0,0" Duration="0:0:0.4"
                                        DecelerationRatio="0.8"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="RESERVATIONS MANAGEMENT" FontSize="16" FontWeight="Bold"
                           Foreground="{StaticResource PrimaryBrush}"/>
                <TextBlock Text="Manage active hold requests, priority queues, and expired holds." FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,12"/>
                <WrapPanel>
                    <StackPanel Margin="0,0,8,8">
                        <TextBlock Text="Search:" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,8,8">
                        <TextBlock Text="Status:" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
                        <ComboBox Width="130" ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding SelectedStatus}"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,8,8">
                        <TextBlock Text="Member Type:" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
                        <ComboBox Width="130" ItemsSource="{Binding MemberTypeOptions}" SelectedItem="{Binding SelectedMemberType}"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,8,8">
                        <TextBlock Text="From:" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
                        <DatePicker Width="120" SelectedDate="{Binding FromDate}"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,8,8">
                        <TextBlock Text="To:" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
                        <DatePicker Width="120" SelectedDate="{Binding ToDate}"/>
                    </StackPanel>
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource PrimaryButton}" Margin="0,12,8,8" Height="28"/>
                    <Button Content="Clear" Command="{Binding ClearFiltersCommand}" Style="{StaticResource SecondaryButton}" Margin="0,12,8,8" Height="28"/>
                    <Button Content="+ New Reservation" Command="{Binding CreateCommand}" ToolTip="Create a reservation after member and title eligibility checks." Style="{StaticResource OperationalButton}" Margin="0,12,8,8" Height="28"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <DataGrid ItemsSource="{Binding Reservations}" SelectedItem="{Binding SelectedReservation}"
                      AutoGenerateColumns="False" IsReadOnly="True" RowHeight="35" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="No." Binding="{Binding ReservationNumber}" Width="110" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Book Title" Binding="{Binding BookTitle}" Width="*"/>
                    <DataGridTextColumn Header="Member" Binding="{Binding MemberName}" Width="150"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding MemberType}" Width="100"/>
                    <DataGridTextColumn Header="Queue" Binding="{Binding QueuePosition}" Width="70" FontWeight="Bold" Foreground="{StaticResource OperationalAccentBrush}"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="Reserved" Binding="{Binding ReservationDate, StringFormat={}{0:dd-MMM-yyyy}}" Width="100" Foreground="{StaticResource TextMutedBrush}"/>
                    <DataGridTextColumn Header="Expires" Binding="{Binding ExpiryDate, StringFormat={}{0:dd-MMM-yyyy}}" Width="100" Foreground="{StaticResource WarningBrush}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,12,0,0">
            <StackPanel>
                <TextBlock Text="ACTIONS" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,8"/>
                <WrapPanel>
                    <TextBox Width="300" Margin="0,0,12,8" Text="{Binding ActionReason, UpdateSourceTrigger=PropertyChanged}" ToolTip="Reason or notes for the action"/>
                    <Button Content="Open Queue" Command="{Binding OpenQueueCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,8"/>
                    <Button Content="Mark Available" Command="{Binding MarkAvailableCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,8"/>
                    <Button Content="Fulfill" Command="{Binding FulfillCommand}" Style="{StaticResource SuccessButton}" Margin="0,0,8,8"/>
                    <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource DangerButton}" Margin="0,0,8,8"/>
                    <Button Content="Expire" Command="{Binding ExpireCommand}" Style="{StaticResource DangerButton}" Margin="0,0,8,8"/>
                </WrapPanel>
                <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="{StaticResource SecondaryBrush}" FontSize="12" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
